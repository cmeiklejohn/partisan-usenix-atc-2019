{mode, max}.

{duration, 2}.

{type, sync_ping}.

{concurrent, 3}.

{driver, lasp_bench_driver_unir}.

{operations, [{sync_ping, 1}]}.

{key_generator, {int_to_bin_bigendian, {uniform_int, 1000}}}.

{value_generator, {fixed_bin, 0}}.