{mode, max}.

{duration, 2}.

{type, object}.

{concurrent, 3}.

{driver, lasp_bench_driver_unir}.

{operations, [{fsm_get, 10}, {fsm_put, 1}]}.

{key_generator, {int_to_bin_bigendian, {uniform_int, 1000}}}.

{value_generator, {fixed_bin, 2097152}}.
