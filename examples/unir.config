{mode, max}.

{duration, 3}.

{concurrent, 10}.

{driver, lasp_bench_driver_unir}.

{operations, [{fsm_get, 1}, {fsm_put, 2}]}.

{key_generator, {int_to_bin_bigendian, {uniform_int, 1000}}}.

{value_generator, {fixed_bin, 100000}}.
